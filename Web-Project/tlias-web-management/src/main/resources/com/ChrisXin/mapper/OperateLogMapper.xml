<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ChrisXin.mapper.OperateLogMapper">

    <select id="list" resultType="com.ChrisXin.entity.OperateLog">
        select l.*, e.name operateEmpName from operate_log l left join emp e on l.operate_emp_id=e.id order by l.operate_time desc
    </select>
</mapper>