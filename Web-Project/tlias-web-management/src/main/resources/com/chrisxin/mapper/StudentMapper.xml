<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chrisxin.mapper.StudentMapper">
    <insert id="add">
            INSERT INTO student(name,no,gender,phone,id_card,is_college,address,degree,graduation_date,clazz_id,violation_count,violation_score,create_time,update_time)
            VALUES(#{name},#{no},#{gender},#{phone},#{idCard},#{isCollege},#{address},#{degree},#{graduationDate},#{clazzId},#{violationCount},#{violationScore},#{createTime},#{updateTime})
    </insert>

    <update id="update">
        UPDATE student
        <set>
                name = #{name},
                no = #{no},
                gender = #{gender},
                phone = #{phone},
                clazz_id = #{clazzId},
                degree = #{degree},
            <if test="idCard != null and idCard!= ''">
                id_card = #{idCard},
            </if>
            <if test="isCollege != null ">
                is_college = #{isCollege},
            </if>
            <if test="address != null and address!= ''">
                address = #{address},
            </if>
            <if test="graduationDate != null">
                graduation_date = #{graduationDate},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="page" resultType="com.chrisxin.entity.Student">
        SELECT
        s.*,c.name clazzName
        FROM student s left join clazz c on s.clazz_id = c.id
        <where>
            <if test="name != null and name !=''">
                AND s.name LIKE concat('%',#{name},'%')
            </if>
            <if test="degree != null and degree !=''">
                AND s.degree = #{degree}
            </if>
            <if test="classId != null and classId !=''">
                AND s.clazz_id = #{classId}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <select id="getStudentCountData" resultType="java.util.Map">
        select clazz.name clazzName,count(*) num from student left join clazz on student.clazz_id = clazz.id group by clazz_id order by num;
    </select>

    <select id="getStudentDegreeData" resultType="java.util.Map">
        select (case degree
            when 1 then '初中'
            when 2 then '高中'
            when 3 then '大专'
            when 4 then '本科'
            when 5 then '硕士'
            when 6 then '博士'
            end) name,count(*) value  from student group by degree;
    </select>
</mapper>